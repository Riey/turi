refresh_rate: 30 seconds
appenders:
    file:
        kind: file
        path: turi.log
        encoder:
            pattern: "{d(%Y-%m-%d %H:%M:%S)} {h([{l}] {f}::{L} - {m})}\n"

root:
    level: trace
    appenders:
        - file

